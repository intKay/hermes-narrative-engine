# 架构设计

## 总体架构

多频道分离设计——叙事在群聊，NPC 在私聊，互不干扰。

```
                    ┌──────────────────────────────────┐
                    │  Main Group Chat（叙事群）        │
                    │                                  │
                    │  [你] ← → [叙述者 Bot A]         │
                    │    （故事叙事 + A/B/C/D 选项）     │
                    │    叙述者不在这里扮演NPC          │
                    └──────────┬───────────────────────┘
                               │ world_state.json
                               │（叙述者每轮更新）
                               ▼
          ┌───────────────────────────────────────────┐
          │          shared world_state.json           │
          │  (所有NPC只读, 每5-10秒轮询)               │
          └────┬──────────────┬─────────────────────┬─┘
               │              │                     │
               ▼              ▼                     ▼
  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
  │ 私聊: 你↔NPC A  │ │ 私聊: 你↔NPC B  │ │ 私聊: 你↔NPC C  │
  │ (陈默-Bot B)    │ │ (李伟-Bot C)    │ │ (林雪-Bot D)    │
  │                 │ │                 │ │                 │
  │ NPC通过world    │ │ NPC通过world    │ │ NPC通过world    │
  │ _state感知剧情  │ │ _state感知剧情  │ │ _state感知剧情  │
  │ 你主动私聊NPC   │ │ 你主动私聊NPC   │ │ 你主动私聊NPC   │
  │ NPC也会主动联系 │ │ NPC也会主动联系  │ │ NPC也会主动联系  │
  └──────────────────┘ └──────────────────┘ └──────────────────┘
```

### 核心原则

- **一个 Bot 只在一个地方说话**——叙述者只在叙事群，NPC 只在各自的私聊
- **世界状态是唯一的纽带**——所有 NPC 通过 `world_state.json` 了解剧情进展，不直接读取群聊消息
- **各 Bot 持有独立 memory**——NPC 记得和你的每一段对话，但不知道群聊里发生了什么

## 模块分层

### 层 1：接入层（Gateway）
- 平台：Telegram，多频道分离
- **叙述者 Bot A**：在叙事群聊中运行，负责故事推进
- **每个 NPC Bot**：在各自的私聊中运行，一个 NPC = 一个独立的 Hermes profile + 一个独立的 Telegram Bot token
- 各 Bot 通过项目根目录下的 `npc/npc-bot-setup.md` 确定自身角色

### 层 2：Master 层（narrative-core Skill）
- 职责：状态路由、存档管理、选项生成、检定执行
- 不负责叙事文本的具体生成
- 每轮输出结构：`[叙事段落] → [A/B/C/D 选项]`
- **仅叙述者 Bot 加载此 Skill**，NPC Bot 不加载

### 层 3：Genre 层（类型插件 Skills）
- 每个类型是一个独立 Skill，遵循统一接口
- 提供：场景素材、检定规则表、口吻控制、结局树
- **仅叙述者 Bot 加载此 Skill**

### 层 4：NPC 层
- 每个 NPC = 一个独立的 Hermes 进程 + 独立的 memory + 独立的私聊会话
- NPC 不加载 narrative-core 或 Genre Skill
- NPC 加载：自己的 NPC 人格 prompt + `npc/shared-state-protocol.md`
- 所有 NPC 通过 `world_state.json` 感知剧情进展
- NPC 可以主动发起对话（通过心跳检测 world_state 变化）

## 数据流（一轮完整交互）

```
玩家选 A
    │
    ▼
Master 收到选项
    │
    ├── 1. 查询当前场景的预置选项表
    │     └── 选项 A → 对应检定：力量 50
    │
    ├── 2. 执行隐形检定（后台掷骰）
    │     └── 成功/失败 → 影响叙事走向
    │
    ├── 3. 更新 world_state
    │     └── 场景 ID、flags、NPC 状态、SAN 变化
    │
    ├── 4. 生成下一段叙事
    │     └── 根据检定结果调整文本
    │
    ├── 5. 生成下一轮选项（A/B/C/D）
    │
    ├── 6. 自动存档（每 5 轮）
    │
    └── 7. 输出到群聊
          └── 叙事段落 + 选项
```

## Bot 角色分配

### 叙述者 Bot A ── Hermes Gateway 主实例

- **所在位置**：叙事群聊（一个群，你和 Bot A 两人）
- **加载文件**：
  - `narrative-core` + 当前 Genre Skill 的 prompt 文件
- **行为逻辑**：
  - 群聊中所有消息都视为玩家输入
  - 生成叙事段落 + A/B/C/D 选项
  - 更新 `world_state.json`（每轮结束后）
  - 执行隐形检定 + Fail Forward
  - 管理存档
  - **不扮演 NPC，不替 NPC 说话**

### NPC Bot B / C / D ── 独立 Hermes 实例（每个一个）

- **所在位置**：各自与你的私聊（一个 NPC = 一个私聊窗口）
- **加载文件**：
  - `npc/NPC-TEMPLATE.md`（该 NPC 的人格定义文件）
  - `npc/shared-state-protocol.md`（读写协议）
- **行为逻辑**：
  - 私聊中玩家的所有消息都视为对该 NPC 说的话
  - 不参与群聊，不读取群聊消息
  - 每 5-10 秒轮询 `world_state.json`，感知剧情进展
  - 当 world_state 更新时，根据 NPC 性格决定是否主动联系玩家
  - 不修改 `world_state.json`
  - **不知道自己是 NPC，不知道自己在一个游戏里**

### 启动方式

```bash
# 叙述者（一个终端）
hermes --profile narrator gateway run

# NPC 陈默（另一个终端）
hermes --profile npc_chenmo gateway run

# NPC 李伟（又一个终端）
hermes --profile npc_liwei gateway run
```

每个 profile 绑定一个不同的 Telegram Bot token。详见 `npc/npc-bot-setup.md`。

---

## 长期注意事项

### 1. 状态漂移防御

随着对话轮数增加，模型对早期设定的遵守度会下降。这是所有大模型的固有特性。

**应对方案**：
- 每 8-10 轮由架构层自动注入一轮「叙事摘要锚点」，将关键状态压缩为 2-3 句注入当前轮次
- 该锚点不存储到存档，只在当前轮次作为上下文锚点出现
- 游戏开始时记录一个「初始设定锚点」，在状态漂移严重时可手动调用 `/reset-context` 重新注入

### 2. 叙事质量天花板由示例密度决定

Genre Skill 的 prompt 中，一段实际的示范文本比十行风格描述更有效。每个 Genre 应至少嵌入 4 段示范：
- 氛围描写（1段）
- NPC 对话（1段）
- 检定成功结果（1段）
- 检定失败 / Fail Forward（1段）

### 3. 双 Bot 的元问题防御

NPC Bot 在群聊中被反复追问时，容易被诱导说出「不应该知道」的信息。必须在 NPC 的 system prompt 中设计明确的拒绝话术。详见 `npc/NPC-TEMPLATE.md` 的「元问题防御」章节。

原则：NPC 不知道自己是 NPC，不知道自己在游戏中。

### 4. 类型隔离深入到 prompt 层

两套类型不只是换主题。检定体系、失败代价、叙事视角必须不同：

| 维度 | 克苏鲁 | 中式怪谈 |
|------|--------|---------|
| 核心代价 | 理智损耗 + 认知崩解 | 阴气上升 + 关系/禁忌破裂 |
| 叙事视角 | 第二人称，逐步侵入 | 第三人称，异常秩序被打破 |
| 信息模型 | 表层→调查层→真相层（认知污染） | 禁忌预兆→违规→惩罚（规则怪谈） |

如果共用同一套检定逻辑，换皮感会很明显。

### 5. 结局数据从第一版开始积累

即使 v0.5 不做「你所不知道的路」的完整展示，存档中也应记录关键节点的未选项。后期做结局回放时数据已就位，不需重新设计存档结构。

存档结构中的 `critical_path` 数组应包含：
```json
{
  "scene": "choose_to_trust",
  "chose": "trust",
  "missed": "distrust",
  "what_if": "如果你当初没有相信陈默……"
}
```
# test multi
