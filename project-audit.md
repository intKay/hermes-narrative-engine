# 项目审计与改进建议

基于对 TRPG/CoC/互动小说从业实践与现有 Bot 实现的调研，对当前 HNE 架构的审计。

---

## 核心发现

当前架构在工程层面（Hermes Skills、存档、双 Bot）设计扎实，但在**叙事设计层面**有若干可优化的点。以下按重要性排序。

---

## 建议一：增加条件化选项系统（高优先级）

**问题**：当前场景的选项是固定的 A/B/C/D，不因玩家已知信息而改变。

**参考来源**：ChoiceScript 的条件化选项机制——只有玩家获取了某条线索后，对应的选项才出现。

**建议**：在 `narrative-core` 的场景数据结构中引入 `condition` 字段（已在 information-control.md 中定义），使选项可动态出现/消失。这直接提升重玩性——你第二次玩同一个故事，因为知道了一些东西，会看到不同的选项。

**改动范围**：
- `narrative-core/SKILL.md`：场景数据格式增加 `condition` 字段
- Master Skill 的轮次处理逻辑：在加载场景数据后，先过滤不可用的选项，再展示
- 存档格式：增加 `player_knowledge` 字段（追踪玩家已获取的线索 ID）

---

## 建议二：引入「你记得……」自动回溯（高优先级）

**问题**：长篇叙事中，玩家会遗忘早期获取的线索。目前没有自动提醒机制。

**参考来源**：Twine 社区的「记忆宏」和 Inkle 的粘性选择——当玩家再次遇到相关线索时，叙事自动插入「你记得之前曾看到过……」

**建议**：在叙事的下一轮生成之前，Master 检查当前场景中是否有与 `player_knowledge` 匹配的条目。如有，自动在叙事开头插入一句：

> 你想起了之前在图书馆读到的那段话——「这座房子建于 1912 年，1914 年曾发生过火灾。」你面前的这扇门把手，看起来比房子本身要新得多。

**改动范围**：
- 在每一轮叙事生成的顶端加入记忆匹配逻辑
- `player_knowledge` 的每条线索需要附带一个「触发场景 ID 列表」——当玩家进入这些场景时，自动触发回溯

---

## 建议三：增加「失败推进」机制（Fail Forward）（中优先级）

**问题**：目前检定失败 = 这条线索断了。这在 TRPG 和互动小说中已被证明是糟糕的设计——它导致玩家卡关。

**参考来源**：CoC 7th 官方建议和 Apocalypse World 的「失败推进」理念——检定失败不是「你没发现任何东西」，而是「你发现了某样东西，但付出了代价」。

**设计**：

```
检定失败不应导向「无信息」，而应导向「有信息 + 代价」：

例1（侦察检定失败）：
  成功：「你注意到书架第二层有一本书的书脊比其他书更突出。」
  失败：「你打翻了书架上的一个摆件。响声在空荡的房间里回荡了很久。你没有找到你想要的东西，但你注意到了——有什么东西在地面上有拖行的痕迹。」

例2（聆听检定失败）：
  成功：「你听到地下深处有微弱的水流声。」
  失败：「你什么也没听到。但你发现——你已经不记得上一分钟自己在想什么了。（SAN -1）」
```

**改动范围**：
- 场景选项表中的 `fail` 字段不应为 null，而应写为「有代价的信息」
- 代价可以包括：SAN 损失、时间流逝、惊动某物、消耗道具

---

## 建议四：叙事节奏控制——三拍法则（中优先级）

**问题**：当前架构中每轮的输出长度没有规范。AI 容易在简短选项中过度展开细节，拖慢节奏。

**参考来源**：CoC KP 指南中的信息分层——「先感官后逻辑」「安全信息制造反差」。

**建议**：固定每轮的叙事长度和结构——三拍法则：

```
第一拍（感官）：1-2 句环境描述（看见、听见、闻到）——always
  例：「门开了。一股潮湿、发霉的气味涌出来。里面很暗。」

第二拍（推进）：0-1 句对上一轮选择的结果反馈——always
  例：「你走进去一步。脚下的地面不完全是实的——有一层积水。」

第三拍（预兆）：0-1 句模糊的未来暗示（Tier 3 戏剧反讽）——occasional
  例：「你没有注意到的是，你身后的门正在自行关闭。」
```

这个结构防止 AI 写出长篇累牍的描述，也防止过于简略。

---

## 建议五：使用 Telegram Thread 作为秘密消息通道（低优先级，双 Bot 阶段）

**参考来源**：Dice Maiden 的秘密掷骰、Telegram TRPGbot 的悄悄话模式。

**建议**：在双 Bot 群聊中，Telegram 的 Thread（话题）功能可以用来处理信息隔离：
- 一个公开 Thread：所有玩家可见的叙事
- 一个私密 Thread（仅玩家 + Bot A）：玩家的隐性信息（检定结果、角色专属线索）
- NPC 专属 Thread（NPC + Bot B）：NPC 的内部决策过程（玩家不可见）

Telegram 的 Thread 本身支持权限控制，不需要额外开发。

---

## 建议六：角色卡应该更厚（低优先级）

**问题**：目前的克苏鲁角色卡只包含 SAN 和少数技能。CoC 经典角色卡的价值在于：它给了角色「锚点」，让玩家在恐怖中有一个可以依赖的身份。

**建议**：扩展角色卡为：

```json
{
  "player_profile": {
    "name": "玩家",
    "occupation": "自由记者",
    "age": 34,
    "backstory": "曾报道过一次工业事故，事故中失去了一个朋友。从那以后对「被掩盖的真相」有执念。",
    "traits": ["固执", "对机械一窍不通", "笔迹工整"],
    "skills": {
      "侦察": 65,
      "聆听": 50,
      "图书馆": 60,
      "说服": 40,
      "心理学": 30
    },
    "inventory": ["录音笔", "折叠刀", "半包烟"],
    "connections": ["马尼拉的一个线人", "大学的旧同事"]
  }
}
```

Occupation + backstory + traits 这三项构成了角色的「身份锚点」。当 SAN 开始下降时，锚点越坚固，玩家越能代入崩溃的过程。

---

## 建议七：结局记录应该包含「你所不知道的」（低优先级）

**问题**：玩家打出一个结局后，往往不知道如果做了不同的选择会看到什么。

**参考来源**：Choice of Games 系列在结局后的「回放模式」——展示关键分岔点的另一条路。

**建议**：结局存档中保留：
```json
{
  "ending": {
    "type": "Bad End - 迷失者",
    "achieved_at": {"scene": "final_basement", "round": 47},
    "critical_junctures": [
      {"scene": "choose_to_trust", "chose": "trust", "missed": "distrust", "what_if": "如果你当初没有相信陈默……"},
      {"scene": "read_the_book", "chose": "read", "missed": "burn", "what_if": "如果你没有读那本书……"}
    ]
  }
}
```

玩家在结局后输入 `/whatif` 可查看所有错过的关键路径。

---

## 建议八：「探索槽位」模式（可选扩展）

**问题**：在复杂场景中（如一栋多层楼房），玩家可能会同时想做多件事。逐轮选择太慢。

**建议**：引入「探索槽位」——当玩家进入一个多区域场景时，给出一组「你可以前往的区域」，玩家每轮选择一个区域探索，未选择区域的状态不会变化（时间暂停）。类似解密游戏中的房间切换。

```
你在一栋三层老宅的一楼。你可以前往：
A. 厨房（未探索）
B. 客厅（未探索）
C. 楼梯口（未探索）
D. 后院（未探索）

[你选了厨房。厨房探索完后，其他区域的状态保持不变。]
```
