# 信息控制机制

## 设计理念

恐怖/悬疑叙事中，「玩家知道什么」本身就是一种资源。信息的不对称、不完整、不可靠——这些是沉浸感的来源，不是缺陷。

HNE 的信息控制分五个层级，和一个核心机制「条件化选项」。

---

## 信息可见性五层

```
层级        │ 名称         │ 谁能看到              │ 示例
────────────┼──────────────┼──────────────────────┼────────────────────────
Tier 0      │ 隐藏信息     │ 只有「世界本身」知道    │ 地下室里有什么 / NPC 的真实动机
Tier 1      │ 可获取信息   │ 玩家通过行动/检定可发现 │ 「书架第二层的书被反复抽动过」
Tier 2      │ 角色知识     │ 角色知道，玩家可查阅    │ 「你是生物学家，这种苔藓只长在石灰岩地区」
Tier 3      │ 戏剧反讽     │ 叙者告诉玩家，角色不知道 │ 「在你背后，那扇门缓缓关上了」
Tier 4      │ NPC 私密     │ 只有 NPC 知道            │ NPC 的秘密 / NPC 看到而玩家没看到的
```

### Tier 0：隐藏信息

这些信息存在于 world_state 中，但在玩家发现之前，叙事中**完全不提及**。

```json
{
  "scene_id": "basement_01",
  "hidden": {
    "behind_the_shelf": "一扇通向更深处的暗门",
    "in_the_darkness": "有一个东西蜷缩在西北角",
    "npc_knows": "陈默知道这个地下室下面还有一层"
  }
}
```

规则：除非玩家通过行动明确探索对应位置，否则隐藏信息永不自动暴露。

### Tier 1：可获取信息

通过有意的行动或检定获取。获取后移入 Tier 2（成为已知信息）。

触发方式：
- 玩家选了一个涉及「检查/观察/搜索」的选项
- 隐形检定通过
- NPC 主动告知

### Tier 2：角色知识

角色知道的信息，玩家在 `/status` 和 `/recall` 中可查看。

包括：
- 已获取的线索列表
- 角色专业技能相关的背景知识（由角色卡决定）
- 已见过的场景和 NPC

### Tier 3：戏剧反讽

叙述者「越界」告诉玩家角色不知道的事。**这是一种权力，必须克制使用。**

使用原则：
- 每 10-15 轮不超过一次
- 只用于张力最强的时刻
- 用特殊格式标记：*斜体 或 【】*

示例：
> *在你背后，你没能看到的地方——那扇门正在缓缓地、无声地关上。*

效果：玩家知道危险在靠近，但角色不知道。玩家会带着焦虑做出下一个选择。

### Tier 4：NPC 私密

NPC 有自己的信息状态。NPC 知道的事情，玩家不一定知道。

NPC 的信息状态存储在 NPC 自己的 state 中：
```json
{
  "npc_name": "陈默",
  "knows": {
    "地下室还有一层": true,
    "那封信是假的": true,
    "玩家在说谎": false
  },
  "shared_with_player": {
    "地下室还有一层": false
  }
}
```

NPC 的信任度决定它是否共享信息。信任度 < 30 时，NPC 有可能主动隐瞒或说谎。

---

## 条件化选项

### 核心规则

选项不再固定。每个选项的出现取决于玩家当前的**已知信息状态**。

```
┌──────────────────────────────────────────┐
│ 你站在一扇半开的门前。                    │
│                                          │
│ 你接下来要怎么做？                        │
│                                          │
│ (始终出现)                               │
│ A. 推门进入                              │
│ B. 敲门                                   │
│ C. 蹲下从门缝里看                        │
│                                          │
│ (仅当玩家已获得线索「门缝下有光」时才出现)  │
│ D. 把纸条从门缝里塞进去                  │
│                                          │
│ (仅当玩家已知「里面有东西」时才出现)       │
│ E. 叫陈默过来一起推门                    │
└──────────────────────────────────────────┘
```

### 实现方式

在场景数据中为每个选项添加 `condition` 字段：

```json
{
  "scene_id": "basement_door",
  "choices": {
    "A": {
      "label": "推门进入",
      "condition": null,          // 无条件，始终出现
      "check": {"skill": "力量", "target": 50},
      "next_scene": "basement_enter"
    },
    "D": {
      "label": "把纸条从门缝里塞进去",
      "condition": {"flag": "clue_light_under_door", "value": true},
      "check": null,
      "next_scene": "push_note"
    },
    "E": {
      "label": "叫陈默过来一起推门",
      "condition": {"npc_trust": ">", 40},
      "check": null,
      "next_scene": "call_chenmo"
    }
  }
}
```

---

## 记忆回溯（/recall）

玩家随时可用 `/recall` 查看当前已知的所有线索和重要信息。

输出格式：
```
你已经知道的事情：
━━━━━━━━━━━━━━━━━━━━
📜 线索：
  · 那扇门下的缝隙透着微光（第3轮）
  · 地下室地板下隐约有水声（第7轮）
  · 陈默提起过这栋房子在1987年停建过一段时间（第10轮）

🧠 你的专业知识：
  · 你是结构工程师——你知道这种地基下面不可能有自然空洞

👤 关于陈默：
  · 信任度：52/100
  · 你知道的：他曾在南方待过三年，具体位置不详
```

---

## /knowledge 命令（全局信息快照）

在群聊中（有 NPC 共存的场景），玩家可通过 `/knowledge` 查看整体信息分布：

```
信息分布：
┌────────────────┬──────┬────────┐
│ 信息区域        │ 你   │ 陈默   │
├────────────────┼──────┼────────┤
│ 地下室入口      │ ✓    │ ✓      │
│ 地板下的水声    │ ✓    │ ✗      │
│ 1987年停建      │ ✓    │ ✓      │
│ 那封信的真伪    │ ✗    │ ?      │
│ 三楼的秘密      │ ✗    │ ✗      │
└────────────────┴──────┴────────┘
```

这个功能在双 Bot 阶段特别有价值——让你知道 NPC 知道什么、你不知道什么，从而决定下一步要和 NPC 说什么、问什么。
